#include <stdio.h>

/**
 * writes a program that print programming is like building a multilingual puzzle
 *
 * hello,puts
 */
int main(void)
{
	puts("\"programming is like building a multilingual puzzle\"");
	return (0);
}
