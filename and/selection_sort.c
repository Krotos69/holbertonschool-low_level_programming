#include <stdio.h>

/**
* selection_sort - search through an array ande keep track of the minimum value during
* each iterarion. at the end of each iterarion, we swap variables.
*/
void slection_sort(int arr[],int n) 
{
	int array[] = { 8, 7, 9, 2, 3, 1, 5, 4, 6,};
for (int i : array)
	{System.out.printf(i)
	}
}
